// ============================================
// src/utils/formatters.js
// ============================================
// Funciones utilitarias para formatear datos
// Incluye:
// - formatTemp(temp, unit): formatea temperatura a °C o °F
// - formatTime(timestamp): convierte Unix timestamp a hora legible
// - formatDate(date): formatea fechas
// - formatSpeed(speed): formatea velocidad del viento
// - capitalizeFirst(string): capitaliza texto

export const formatTemp = (temp, unit = 'C') => {
  // Implementación pendiente
  return null;
};

export const formatTime = (timestamp) => {
  // Implementación pendiente
  return null;
};

export const formatDate = (date) => {
  // Implementación pendiente
  return null;
};